---
- name: Install OpAMP supervisor on Linux
  ansible.builtin.include_tasks: linux.yml
  when: ansible_facts['os_family'] != "Windows"

- name: Install OpAMP supervisor on Windows
  ansible.builtin.include_tasks: windows.yml
  when: ansible_facts['os_family'] == "Windows"
